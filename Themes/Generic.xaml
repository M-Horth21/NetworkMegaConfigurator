<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:NetworkMegaConfigurator">


  <Style TargetType="{x:Type local:QuickAction}">
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:QuickAction}">
          <Grid>
            <Border Background="#114B9A"
                Width="200" 
                 Height="50"
                CornerRadius="25"
                Padding="25,0,25,0">
              <StackPanel VerticalAlignment="Center">
                <TextBlock x:Name="adapterConfig" FontSize="16"
                       Foreground="#CCFFFFFF"
                           Text="{TemplateBinding Setting}"></TextBlock>
                <TextBlock x:Name="adapterName" FontSize="12"
                       Foreground="#CCFFFFFF"
                           Text="{TemplateBinding Description}"></TextBlock>
              </StackPanel>
            </Border>
          </Grid>

          <ControlTemplate.Triggers>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>
